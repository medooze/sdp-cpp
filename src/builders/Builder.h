#ifndef _SDP_IMPL_BUILDER_H_
#define _SDP_IMPL_BUILDER_H_

#include <vector>
#include "../abnf/Rule.hpp"
#include "../abnf/Visitor.hpp"

using namespace abnf;

class Builder : 
	public Visitor 
{
public:
	 virtual void* visit(const Rule_session_description* rule);
	 virtual void* visit(const Rule_proto_version* rule);
	 virtual void* visit(const Rule_origin_field* rule);
	 virtual void* visit(const Rule_session_name_field* rule);
	 virtual void* visit(const Rule_information_field* rule);
	 virtual void* visit(const Rule_uri_field* rule);
	 virtual void* visit(const Rule_email_fields* rule);
	 virtual void* visit(const Rule_email_field* rule);
	 virtual void* visit(const Rule_phone_fields* rule);
	 virtual void* visit(const Rule_phone_field* rule);
	 virtual void* visit(const Rule_connection_field* rule);
	 virtual void* visit(const Rule_bandwidth_fields* rule);
	 virtual void* visit(const Rule_bandwidth_field* rule);
	 virtual void* visit(const Rule_time_fields* rule);
	 virtual void* visit(const Rule_time_repeat_field* rule);
	 virtual void* visit(const Rule_time_field* rule);
	 virtual void* visit(const Rule_repeat_field* rule);
	 virtual void* visit(const Rule_zone_adjustments* rule);
	 virtual void* visit(const Rule_key_field* rule);
	 virtual void* visit(const Rule_attribute_fields* rule);
	 virtual void* visit(const Rule_attribute_field* rule);
	 virtual void* visit(const Rule_gen_attribute* rule);
	 virtual void* visit(const Rule_sctpmap_attr* rule);
	 virtual void* visit(const Rule_sctpmap_number* rule);
	 virtual void* visit(const Rule_app* rule);
	 virtual void* visit(const Rule_streams* rule);
	 virtual void* visit(const Rule_fingerprint_attribute* rule);
	 virtual void* visit(const Rule_hash_func* rule);
	 virtual void* visit(const Rule_fingerprint* rule);
	 virtual void* visit(const Rule_extmap_attribute* rule);
	 virtual void* visit(const Rule_extension_identifier* rule);
	 virtual void* visit(const Rule_extension_name* rule);
	 virtual void* visit(const Rule_direction* rule);
	 virtual void* visit(const Rule_extension_attributes* rule);
	 virtual void* visit(const Rule_candidate_attribute* rule);
	 virtual void* visit(const Rule_foundation* rule);
	 virtual void* visit(const Rule_component_id* rule);
	 virtual void* visit(const Rule_transport* rule);
	 virtual void* visit(const Rule_transport_extension* rule);
	 virtual void* visit(const Rule_priority* rule);
	 virtual void* visit(const Rule_cand_type* rule);
	 virtual void* visit(const Rule_candidate_types* rule);
	 virtual void* visit(const Rule_rel_addr* rule);
	 virtual void* visit(const Rule_rel_port* rule);
	 virtual void* visit(const Rule_extension_att* rule);
	 virtual void* visit(const Rule_extension_att_name* rule);
	 virtual void* visit(const Rule_extension_att_value* rule);
	 virtual void* visit(const Rule_ice_char* rule);
	 virtual void* visit(const Rule_crypto_attribute* rule);
	 virtual void* visit(const Rule_tag* rule);
	 virtual void* visit(const Rule_crypto_suite* rule);
	 virtual void* visit(const Rule_key_params* rule);
	 virtual void* visit(const Rule_key_param* rule);
	 virtual void* visit(const Rule_key_method* rule);
	 virtual void* visit(const Rule_key_method_ext* rule);
	 virtual void* visit(const Rule_key_info* rule);
	 virtual void* visit(const Rule_key_salt* rule);
	 virtual void* visit(const Rule_key_lifetime* rule);
	 virtual void* visit(const Rule_key_mki* rule);
	 virtual void* visit(const Rule_key_mki_value* rule);
	 virtual void* visit(const Rule_key_mki_length* rule);
	 virtual void* visit(const Rule_session_param* rule);
	 virtual void* visit(const Rule_mid_attr* rule);
	 virtual void* visit(const Rule_identification_tag* rule);
	 virtual void* visit(const Rule_group_attr* rule);
	 virtual void* visit(const Rule_group_semantics* rule);
	 virtual void* visit(const Rule_ssrc_attr* rule);
	 virtual void* visit(const Rule_ssrc_id* rule);
	 virtual void* visit(const Rule_ssrc_group_attr* rule);
	 virtual void* visit(const Rule_ssrc_group_semantics* rule);
	 virtual void* visit(const Rule_cname_attr* rule);
	 virtual void* visit(const Rule_cname* rule);
	 virtual void* visit(const Rule_previous_ssrc_attr* rule);
	 virtual void* visit(const Rule_rtpmap_attr* rule);
	 virtual void* visit(const Rule_name* rule);
	 virtual void* visit(const Rule_rate* rule);
	 virtual void* visit(const Rule_parameters* rule);
	 virtual void* visit(const Rule_fmtp_attr* rule);
	 virtual void* visit(const Rule_param_list* rule);
	 virtual void* visit(const Rule_media_descriptions* rule);
	 virtual void* visit(const Rule_media_description* rule);
	 virtual void* visit(const Rule_media_field* rule);
	 virtual void* visit(const Rule_username* rule);
	 virtual void* visit(const Rule_sess_id* rule);
	 virtual void* visit(const Rule_sess_version* rule);
	 virtual void* visit(const Rule_nettype* rule);
	 virtual void* visit(const Rule_addrtype* rule);
	 virtual void* visit(const Rule_email_address* rule);
	 virtual void* visit(const Rule_address_and_comment* rule);
	 virtual void* visit(const Rule_dispname_and_address* rule);
	 virtual void* visit(const Rule_phone_number* rule);
	 virtual void* visit(const Rule_phone* rule);
	 virtual void* visit(const Rule_connection_address* rule);
	 virtual void* visit(const Rule_unicast_address* rule);
	 virtual void* visit(const Rule_bwtype* rule);
	 virtual void* visit(const Rule_bandwidth* rule);
	 virtual void* visit(const Rule_start_time* rule);
	 virtual void* visit(const Rule_stop_time* rule);
	 virtual void* visit(const Rule_time* rule);
	 virtual void* visit(const Rule_repeat_interval* rule);
	 virtual void* visit(const Rule_typed_time* rule);
	 virtual void* visit(const Rule_fixed_len_time_unit* rule);
	 virtual void* visit(const Rule_key_type* rule);
	 virtual void* visit(const Rule_prompt_key_type* rule);
	 virtual void* visit(const Rule_clear_key_type* rule);
	 virtual void* visit(const Rule_base64_key_type* rule);
	 virtual void* visit(const Rule_uri_key_type* rule);
	 virtual void* visit(const Rule_base64* rule);
	 virtual void* visit(const Rule_base64_unit* rule);
	 virtual void* visit(const Rule_base64_pad* rule);
	 virtual void* visit(const Rule_base64_char* rule);
	 virtual void* visit(const Rule_attribute* rule);
	 virtual void* visit(const Rule_att_field* rule);
	 virtual void* visit(const Rule_att_value* rule);
	 virtual void* visit(const Rule_media* rule);
	 virtual void* visit(const Rule_fmt* rule);
	 virtual void* visit(const Rule_proto* rule);
	 virtual void* visit(const Rule_port* rule);
	 virtual void* visit(const Rule_number_of_ports* rule);
	 virtual void* visit(const Rule_IP4_multicast* rule);
	 virtual void* visit(const Rule_m1* rule);
	 virtual void* visit(const Rule_IP6_multicast* rule);
	 virtual void* visit(const Rule_ttl* rule);
	 virtual void* visit(const Rule_FQDN* rule);
	 virtual void* visit(const Rule_email_safe* rule);
	 virtual void* visit(const Rule_URI* rule);
	 virtual void* visit(const Rule_hier_part* rule);
	 virtual void* visit(const Rule_URI_reference* rule);
	 virtual void* visit(const Rule_absolute_URI* rule);
	 virtual void* visit(const Rule_relative_ref* rule);
	 virtual void* visit(const Rule_relative_part* rule);
	 virtual void* visit(const Rule_scheme* rule);
	 virtual void* visit(const Rule_authority* rule);
	 virtual void* visit(const Rule_userinfo* rule);
	 virtual void* visit(const Rule_host* rule);
	 virtual void* visit(const Rule_IP_literal* rule);
	 virtual void* visit(const Rule_IPvFuture* rule);
	 virtual void* visit(const Rule_IPv6address* rule);
	 virtual void* visit(const Rule_h16* rule);
	 virtual void* visit(const Rule_ls32* rule);
	 virtual void* visit(const Rule_IPv4address* rule);
	 virtual void* visit(const Rule_dec_octet* rule);
	 virtual void* visit(const Rule_reg_name* rule);
	 virtual void* visit(const Rule_path* rule);
	 virtual void* visit(const Rule_path_abempty* rule);
	 virtual void* visit(const Rule_path_absolute* rule);
	 virtual void* visit(const Rule_path_noscheme* rule);
	 virtual void* visit(const Rule_path_rootless* rule);
	 virtual void* visit(const Rule_path_empty* rule);
	 virtual void* visit(const Rule_segment* rule);
	 virtual void* visit(const Rule_segment_nz* rule);
	 virtual void* visit(const Rule_segment_nz_nc* rule);
	 virtual void* visit(const Rule_pchar* rule);
	 virtual void* visit(const Rule_query* rule);
	 virtual void* visit(const Rule_fragment* rule);
	 virtual void* visit(const Rule_pct_encoded* rule);
	 virtual void* visit(const Rule_unreserved* rule);
	 virtual void* visit(const Rule_reserved* rule);
	 virtual void* visit(const Rule_gen_delims* rule);
	 virtual void* visit(const Rule_sub_delims* rule);
	 virtual void* visit(const Rule_addr_spec* rule);
	 virtual void* visit(const Rule_local_part* rule);
	 virtual void* visit(const Rule_domain* rule);
	 virtual void* visit(const Rule_domain_literal* rule);
	 virtual void* visit(const Rule_dcontent* rule);
	 virtual void* visit(const Rule_dtext* rule);
	 virtual void* visit(const Rule_NO_WS_CTL* rule);
	 virtual void* visit(const Rule_quoted_pair* rule);
	 virtual void* visit(const Rule_text* rule);
	 virtual void* visit(const Rule_FWS* rule);
	 virtual void* visit(const Rule_comment* rule);
	 virtual void* visit(const Rule_ccontent* rule);
	 virtual void* visit(const Rule_ctext* rule);
	 virtual void* visit(const Rule_dot_atom* rule);
	 virtual void* visit(const Rule_dot_atom_text* rule);
	 virtual void* visit(const Rule_atext* rule);
	 virtual void* visit(const Rule_integer* rule);
	 virtual void* visit(const Rule_alpha_numeric* rule);
	 virtual void* visit(const Rule_decimal_uchar* rule);
	 virtual void* visit(const Rule_decimal_uchar_1xx* rule);
	 virtual void* visit(const Rule_decimal_uchar_2xx* rule);
	 virtual void* visit(const Rule_decimal_uchar_25x* rule);
	 virtual void* visit(const Rule_BIT* rule);
	 virtual void* visit(const Rule_CHAR* rule);
	 virtual void* visit(const Rule_CR* rule);
	 virtual void* visit(const Rule_CTL* rule);
	 virtual void* visit(const Rule_HEXDIG* rule);
	 virtual void* visit(const Rule_HTAB* rule);
	 virtual void* visit(const Rule_LF* rule);
	 virtual void* visit(const Rule_LWSP* rule);
	 virtual void* visit(const Rule_OCTET* rule);
	 virtual void* visit(const Rule_SP* rule);
	 virtual void* visit(const Rule_VCHAR* rule);
	 virtual void* visit(const Rule_quoted_string* rule);
	 virtual void* visit(const Rule_qcontent* rule);
	 virtual void* visit(const Rule_qtext* rule);
	 virtual void* visit(const Rule_obs_local_part* rule);
	 virtual void* visit(const Rule_obs_domain* rule);
	 virtual void* visit(const Rule_obs_FWS* rule);
	 virtual void* visit(const Rule_obs_qp* rule);
	 virtual void* visit(const Rule_obs_text* rule);
	 virtual void* visit(const Rule_obs_char* rule);
	 virtual void* visit(const Rule_CFWS* rule);
	 virtual void* visit(const Rule_WSP* rule);
	 virtual void* visit(const Rule_atom* rule);
	 virtual void* visit(const Rule_word* rule);
	 virtual void* visit(const Rule_extn_addr* rule);
	 virtual void* visit(const Rule_non_ws_string* rule);
	 virtual void* visit(const Rule_byte_string* rule);
	 virtual void* visit(const Rule_CRLF* rule);
	 virtual void* visit(const Rule_token* rule);
	 virtual void* visit(const Rule_ALPHA* rule);
	 virtual void* visit(const Rule_DIGIT* rule);
	 virtual void* visit(const Rule_POS_DIGIT* rule);
	 virtual void* visit(const Rule_DQUOTE* rule);
	 virtual void* visit(const Rule_EQUALS* rule);

	 virtual void* visit(const Terminal_StringValue* value);
	 virtual void* visit(const Terminal_NumericValue* value);
	 
protected:
	void* visitRules(const std::vector<const Rule*>& rules);	 
};

#endif
